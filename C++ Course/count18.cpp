#include <iostream>
#include <climits>
using namespace std;

int main()
{

    
    // // sorting an array in asscending order 
    // int n;
    // cout << "Enter a number = \n";
    // cin >> n;
    // int arr[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> arr[i];
    // }
    // for (int i = 0; i < n - 1; i++)
    // {
    //     for (int j = i + 1; j < n; j++)
    //     {
    //         if (arr[j] < arr[i])
    //         {
    //             int temp = arr[j];
    //             arr[j] = arr[i];
    //             arr[i] = temp;
    //         }
    //     }
    // }
    // for (int i = 0; i < n; i++)
    // {
    //     cout << arr[i] << " ";
    // }

    // Maximum or Minimum of the array is
    // int n;
    // cout<<"Enter a number = ";
    // cin >> n;
    // int a[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> a[i];
    // }
    // int maxNo = INT_MIN;
    // int minNo = INT_MAX;
    // for (int i = 0; i < n; i++)
    // {
    //     maxNo = max(maxNo, a[i]);
    //     minNo = min(minNo, a[i]);
    // }
    // cout << maxNo << " " << minNo;
}